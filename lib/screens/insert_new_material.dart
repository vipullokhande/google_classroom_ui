import 'package:flutter/material.dart';

class InsertNewMaterial extends StatelessWidget {
  const InsertNewMaterial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
